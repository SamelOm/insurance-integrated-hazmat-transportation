# Data

`master_data.xlsx` is the processed input workbook used by every optimization script. The `intermediate/` folder preserves upstream path-risk and risk-attribution tables, while `results/` contains previously generated sensitivity outputs.

The raw geospatial construction pipeline is not included. See [`docs/data_dictionary.md`](../docs/data_dictionary.md) for schemas, row counts, and known limitations.
